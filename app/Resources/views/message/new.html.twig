{% block body %}
    <div class="publish-message-form">
        <h1>Publier un message</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Publier" class="btn btn-primary btn-md" style="margin: 5px 0px" />
        {{ form_end(form) }}
    </div>
{% endblock %}
